; Export presets. Web is the only target that matters here -- the desktop
; build runs from source.
;
; NOTE: this cannot be exported with the Mono editor. Godot 4 refuses a web
; export from a C#/.NET build even when the project has no C# in it, and says
; so: "use a non-C# editor build to export the project". Use the standard
; Godot 4.7.2, which is also what CI uses.
;
; thread_support is off on purpose. Threaded web builds require the page to be
; cross-origin isolated (COOP/COEP headers), which rules out most static
; hosting; nothing here uses threads, so the no-threads template avoids the
; whole problem.

[preset.0]

name="Web"
platform="Web"
runnable=true
advanced_options=false
dedicated_server=false
custom_features=""
export_filter="all_resources"
include_filter=""
exclude_filter="tests/*, reports/*, docs/*, tools/*, addons/gut/*, *.md, *.sh"
export_path="build/web/index.html"
encryption_include_filters=""
encryption_exclude_filters=""
encrypt_pck=false
encrypt_directory=false
script_export_mode=2

[preset.0.options]

custom_template/debug=""
custom_template/release=""
variant/extensions_support=false
variant/thread_support=false
vram_texture_compression/for_desktop=false
vram_texture_compression/for_mobile=false
html/export_icon=true
html/custom_html_shell=""
html/head_include=""
html/canvas_resize_policy=2
html/focus_canvas_on_start=true
html/experimental_virtual_keyboard=true
progressive_web_app/enabled=false
